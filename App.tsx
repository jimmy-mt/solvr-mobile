// Expo Router is the app entry now. This file is intentionally kept as a
// compatibility placeholder so older imports fail gracefully during migration.
export default function App() {
  return null;
}
